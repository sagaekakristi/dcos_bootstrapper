- on bootstrapper

sudo su
cd /home/developer/ && sudo yum install git -y && git clone https://github.com/sagaekakristi/dcos_bootstrapper.git dcos_bootstrapper && cd dcos_bootstrapper

echo "192.168.0.xxx" > /home/developer/ip
sudo chmod 755 *.sh ip-detect

sh bootstrap.sh

sudo ssh-keygen -t rsa
sh sshkeys_bootstrap.sh

cat ~/.ssh/id_rsa.pub

reboot

cd /home/developer/
sh dcos_bootstrapper/download_dcos_installer.sh
sh dcos_bootstrapper/start_dcos_installer.sh

- on agent: run bootstrapper script

sudo yum install git -y && git clone https://github.com/sagaekakristi/dcos_bootstrapper.git dcos_bootstrapper && cd dcos_bootstrapper && sudo chmod 755 *.sh ip-detect

sudo ssh-keygen -t rsa
nano id_rsa.pub
<copy content>

sh bootstrap.sh && sh agent_bootstrap.sh && reboot